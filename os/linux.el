;; Linux

;;-------------------------
;; font
;;-------------------------
;; Initial frame settings
(setq default-frame-alist
      (append (list
              '(font . "DejaVu Sans Mono Book"))
              default-frame-alist))

